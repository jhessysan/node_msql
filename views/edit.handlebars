<h2>Editar livro</h2>

<form action="/edit/save" method="POST">
    <input type="hidden" value="{{book.id}}" name="id">

    
    <div>
        <label for="title">Titulo:</label>
        <input type="text" name="title" placeholder="Digite o titulo" id="title" value="{{book.title}}">
    </div>
    <div>
        <label for="pageqty">Números de paginas:</label>
        <input type="text" name="pageqty" placeholder="Quantas paginas o livro possui:" id="pageqty" value="{{book.pageqty}}">
    </div>

    <button type="submit">Enviar</button>
</form>